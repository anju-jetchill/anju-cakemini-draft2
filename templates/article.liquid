{%- render 'page-hero' -%}
{%- render 'richsnippet-article' -%}
{%- render 'breadcrumbs' -%}

<div class="page">
    <div class="page__wrap container--narrow">
        {{ article.content }}
    </div>
</div>
